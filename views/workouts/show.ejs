<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type='text/css' href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@700&display=swap" rel="stylesheet">

    <title>Workout Tracker | See Workout Details</title>
</head>
<body>
    <div class="top-nav">
        <nav>
            <a href="/">Home</a>
 
            <a href="/workouts">Workout Index</a>

        </nav>
    </div>   
    <h1><%= workout.name%></h1>

    <main>


        <h2>Workout Details</h2>
        <a href="/exercises/new">
        <button class="button">Add Exercise</button></a>
        <section class="workout-show-container">

            <ul>
                <% exercises.forEach(exercise => { %>
                    <h3> <a href="/exercises/<%=exercise._id%>"><%=exercise.name%></a></h3>
                    <li>
                        Sets: <%= exercise.sets %>,
                        Reps: <%= exercise.reps %>,
                        Weight: <%= exercise.weight %> lbs
                    </li>
                    
                <% }); %>
            </ul>
        </section>
        <section>
            <h2>Actions</h2>
            <% if(currentUser) { %>
                <a href="/workouts/<%= workout._id %>/edit"><button class="button">Edit Workout</button></a>
                <form action="/workouts/<%= workout._id %>?_method=DELETE" method="POST" style="padding-top: 10px;">
                <input class="button" type="submit" value="Delete Workout">
                </form>

            <% } else { %>
                <p>Sorry, you don't have access to these actions. Please log in to continue.</p>
            <% } %>
        </section>
    </main> 
</body>
</html>