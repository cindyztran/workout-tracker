<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Workout Tracker | <%= exercise.name %></title>
</head>
<body>
    <header>
        <h1><%= exercise.name %></h1>
        <nav>
            <ul>
                <li>
                    <a href="/">Home</a>
                </li>
                <li>
                    <a href="/exercises">Exercises Index</a>
                </li>
            </ul>
        </nav>
    </header>
    <main>
        <section>
            <h5>Workout: <%= exercise.createdFor.name%> on <%= new Date(exercise.createdAt).toLocaleDateString() %></h5>
            <label for="sets">Sets:</label>
            <%= exercise.sets%><br/>
            <label for="reps">Reps:</label>
            <%= exercise.reps%><br/>
            <label for="weight">Weight:</label>
            <%= exercise.weight %> lbs<br/>
            
        </section>
        <section>
            <h2>Actions</h2>
            <% if(currentUser) { %>

                <a href="/exercises/<%= exercise._id %>/edit"><button class="button">Edit Exercise</button></a>
                <form action="/exercises/<%= exercise._id%>?_method=DELETE" method="POST">
                <input type="submit" value="Delete Exercise">
                </form>

            <% } else { %>
                <p>Sorry, you don't have access to these actions. Please log in to continue.</p>
            <% } %>
        </section>
    </main>
</body>
</html>